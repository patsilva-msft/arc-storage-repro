name: ARC PVC Test

on:
  workflow_dispatch:

jobs:
  runner-load:
    runs-on: arc-storage-repro

    steps:
      - run: |
          mkdir -p /home/runner/_work/test
          dd if=/dev/zero \
             of=/home/runner/_work/test/file.bin \
             bs=1M count=500

          sleep 300

